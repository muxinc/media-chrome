{
  "name": "player-chrome",
  "version": "0.0.1",
  "description": "Custom elements (web components) for making audio and video player controls that look great in your website or app.",
  "main": "dist/components/player-chrome.js",
  "scripts": {
    "build": "rm -rf dist && parcel build js/*.js --out-dir dist/components && parcel build examples/*.html --out-dir dist/examples --no-minify",
    "dev": "parcel index.html examples/*.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/muxinc/player-chrome.git"
  },
  "keywords": [
    "webcomponent",
    "html5",
    "video",
    "player",
    "controls"
  ],
  "author": "Mux, Inc.",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/muxinc/player-chrome/issues"
  },
  "homepage": "https://github.com/muxinc/player-chrome#readme",
  "devDependencies": {
    "parcel": "^1.12.3"
  },
  "dependencies": {
    "hls.js": "^0.12.4",
    "raw-loader": "^3.1.0",
    "video.js": "^7.6.5"
  }
}
